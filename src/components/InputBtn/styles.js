import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({
  InputSearch: {
    fontSize: 22,
    backgroundColor: '#fff',
  },
  InpContainer: {
    marginTop: 60,
  },
});

export default Styles;
